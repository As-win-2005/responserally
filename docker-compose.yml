version: "3"

services:

  backend:
    build: ./backend
    container_name: responserally_backend
    ports:
      - "5000:5000"
    env_file:
      - .env

  frontend:
    build: ./frontend
    container_name: responserally_frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend
